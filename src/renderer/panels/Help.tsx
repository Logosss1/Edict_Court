import { Markdown } from '../common/Markdown';

const HELP = `# 使用说明

## 两种模式（⌘J 切换）
- **工作台**：Cursor 式下旨输入框（⌘L 显示/隐藏，⌘I 聚焦）、军机处面板、Editor（文件树 / 多标签 / 语法高亮 / 全局搜索替换 / 集成终端 / 问题面板 / Git）。
- **朝堂**：像素唐风朝堂。你是皇上，端坐太和殿龙椅。点击官员小人查看其当前工作、留朱批；奏折送到御前时点击即可「奏折批阅」。
两种模式共享同一个运行时：任务、审批、结果完全一致、实时同步。

## 协同三档
| 档位 | 流程 | 适用 |
|---|---|---|
| Solo | 独相直接对话、读写文件、执行命令 | 简单任务，最省 token |
| Court Lite | 太子分拣 → 中书规划 → 门下审议（封驳强制返工，超限升级御裁）→ 六部执行 → 回奏 | 日常任务 |
| Full Court | 太子 / 中书 / 门下 / 尚书 / 六部全流程；可选朝堂议政；方案御览（可朱笔涂改）；六部并行；门下审议成果；御批结案 | 复杂长任务 |

## 实时干预
- **叫停 / 恢复 / 取消**：看板卡片、Agent 面板、朝堂均可操作。执行中或审议中的取消属高风险流转，须皇上确认。
- **朱批**：给任意官员留言，其下一轮必须读到并在回复开头回应（旨意详情 → 朱批）。
- **插话**：朝堂议政中直接下口谕，官员会针对你的发言继续辩论。
- **涂改方案**：Full Court 中门下准奏后进入「御览方案」，可直接修改子任务、部门、验收标准后放行，流程按修改后的方案执行。
- **局部重试**：节点失败后旨意进入「阻塞」，在旨意详情里对失败节点「局部重试」，不重放已完成节点。

## 安全
- Agent 只能访问所打开的工作区；符号链接越界、\`.git\` 内部改写被拒绝。
- 高风险命令（递归删除、sudo、git push、网络外发、安装依赖等）与敏感文件读写始终需要确认。
- API Key 经系统钥匙串加密，不进入日志 / 审计 / 导出；外发请求只发往你配置的服务。
- 审计日志为 SHA-256 哈希链，可在「审计日志」中校验。

## 快捷键
⌘J 切换模式 · ⌘L 显示/隐藏输入框 · ⌘I 聚焦输入框 · ⌘P 快速打开 · ⇧⌘P 命令面板 · ⇧⌘F 全局搜索 · ⌃\` 终端 · ⌘B 侧栏 · ⌘S 保存 · ⌘W 关闭标签 · ⇧⌘K 看板 · ⇧⌘U 上朝 · ⌘1–4 朝堂场景
`;

export function Help() {
  return (
    <div className="panel help">
      <Markdown text={HELP} />
    </div>
  );
}
